services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: funding-bot:latest
    command: ["python", "-m", "funding_bot", "send-daily-summary"]
    env_file:
      - .env
    volumes:
      - bot_data:/app/data

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    image: funding-bot:latest
    profiles: ["queue"]
    command:
      [
        "celery",
        "--app",
        "task_queue.celery_app",
        "worker",
        "--loglevel=info",
        "--queues",
        "funding-bot",
      ]
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - bot_data:/app/data

  flower:
    build:
      context: .
      dockerfile: Dockerfile
    image: funding-bot:latest
    profiles: ["queue"]
    command:
      [
        "celery",
        "--app",
        "task_queue.celery_app",
        "flower",
        "--port=5555",
      ]
    env_file:
      - .env
    depends_on:
      - redis
      - worker
    ports:
      - "5555:5555"
    volumes:
      - bot_data:/app/data

  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: funding-bot:latest
    command:
      [
        "python",
        "-m",
        "flask",
        "--app",
        "web.app",
        "run",
        "--host",
        "0.0.0.0",
        "--port",
        "5000",
      ]
    env_file:
      - .env
    depends_on:
      - bot
    ports:
      - "5000:5000"
    volumes:
      - bot_data:/app/data

  redis:
    image: redis:7-alpine
    profiles: ["queue"]
    ports:
      - "6379:6379"

volumes:
  bot_data:
